# Just a statistic, not used in gameplay
scoreboard players reset @s fwdied

# Used for teleport cooldowns
scoreboard players reset @s fwcountdown

# Used internally by tests
# But because this is also used by tests, don't actually reset it
# scoreboard players reset @s fwtest
